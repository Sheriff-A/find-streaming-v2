spring.application.name=backend

# no default profile - pick one explicitly so you always know which DB you're
# pointing at: `local` (backend on host, db in Docker) or `docker` (both in Docker)
spring.profiles.active=${SPRING_PROFILES_ACTIVE:local}
